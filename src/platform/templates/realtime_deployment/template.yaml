capability: realtime_deployment
components:
  online_endpoint_assets:
    files:
      - { src: endpoint.yml.j2, dest: aml/deployment/endpoint.yml }
      - { src: deployment.yml.j2, dest: aml/deployment/deployment.yml }
  scoring_code:
    files:
      - { src: score.py.j2, dest: src/score.py }
  azdo_cd_realtime:
    files:
      - { src: cd-realtime.yml.j2, dest: azdopipelines/cd-realtime.yml }
