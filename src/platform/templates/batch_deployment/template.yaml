capability: batch_deployment
components:
  batch_endpoint_assets:
    files:
      - { src: batch_endpoint.yml.j2, dest: aml/deployment/batch_endpoint.yml }
      - { src: batch_deployment.yml.j2, dest: aml/deployment/batch_deployment.yml }
  inference_code:
    files:
      - { src: batch_score.py.j2, dest: src/batch_score.py }
  azdo_cd_batch:
    files:
      - { src: cd-batch.yml.j2, dest: azdopipelines/cd-batch.yml }
